# AthensGo Mobile App - Environment Configuration

# Backend API URL
# For local development, use localhost
# For physical devices, use your machine's IP or ngrok URL
API_BASE_URL=http://localhost:3001

# Google Maps API Key
# Get your key from: https://console.cloud.google.com/google/maps-apis/credentials
# Make sure to enable Maps SDK for Android and Maps SDK for iOS
GOOGLE_MAPS_API_KEY=YOUR_GOOGLE_MAPS_API_KEY_HERE

# Note: Expo doesn't support .env files by default
# These are just for reference. Update values directly in:
# - services/api.ts (API_BASE_URL)
# - app.json (GOOGLE_MAPS_API_KEY)
